/**
 * @file FormItem 物料的编辑态(dev)组件:占位组件,自身不渲染内容。
 * 其配置(label/name/type/rules)由父级 Form 通过 React.Children 读取后统一渲染。
 */

/**
 * FormItem 物料的编辑态(dev)占位组件,自身不渲染内容
 */
export const FormItem = () => {
  return <div></div>
}
