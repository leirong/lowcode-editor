/**
 * @file FormItem 物料的生产态(prod)组件:占位组件,自身不渲染内容。
 * 其配置由父级 Form 通过 React.Children 读取后统一渲染为真实的表单项。
 */

/**
 * FormItem 物料的生产态(prod)占位组件,自身不渲染内容
 */
export const FormItem = () => {
  return <div></div>
}
