/**
 * @file TableColumn 物料的生产态(prod)组件:占位组件,自身不渲染内容。
 * 其配置由父级 Table 通过 React.Children 读取后生成真实的表格列。
 */

/**
 * TableColumn 物料的生产态(prod)占位组件,自身不渲染内容
 */
export const TableColumn = () => {
  return <></>
}
