/**
 * @file TableColumn 物料的编辑态(dev)组件:占位组件,自身不渲染内容。
 * 其配置(title/dataIndex/type)由父级 Table 通过 React.Children 读取后生成表格列。
 */

/**
 * TableColumn 物料的编辑态(dev)占位组件,自身不渲染内容
 */
export const TableColumn = () => {
  return <></>
}
